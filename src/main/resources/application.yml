spring:
  application:
    name: auction_app
  datasource:
    url: jdbc:postgresql://localhost:5432/auction_app
    username: postgres
    password: gokul@2006
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB 
  mail:
      host: smtp-relay.brevo.com
      port: 587
      username: 86bce5001@smtp-brevo.com
      password: JhkxNfrFAydMZVsQ
      properties:
        mailsmtp.auth: true
        mail.smtp.starttls.enable: true
        mail.smtp.starttls.required: true   
server:
  servlet:
    context-path: /auction_app
file:
  upload-dir : ./upload